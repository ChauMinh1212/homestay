pipeline {
    agent {
        docker {
            image 'node:14-alpine'  // Image Docker muốn sử dụng
        }
    }
    stages {
        stage('Check Docker') {
            steps {
                script {
                    sh 'docker --version || echo "Docker is not installed or accessible"'
                }
            }
        }
    }
}