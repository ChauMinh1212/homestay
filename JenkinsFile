pipeline {
    agent any
    stages {
        stage('Check Docker') {
            steps {
                script {
                    sh 'docker --version || echo "Docker is not installed or accessible"'
                }
            }
        }
    }
}