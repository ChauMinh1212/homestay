pipeline {
    agent any
    stages {
        stage('Build and push docker image') {
            steps {
                script {
                    sh "bash build.sh"
                }
            }
        }
    }
}